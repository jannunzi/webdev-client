{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://webdev-client.vercel.app/schemas/lecture-clips.schema.json",
  "title": "Book section lecture clips",
  "description": "Derived map from a book TOC id to YouTube clips. Do not store Zoom, Box, or other archive files. Student playback is YouTube-only.",
  "type": "object",
  "additionalProperties": false,
  "required": ["sections"],
  "properties": {
    "$schema": { "type": "string" },
    "description": {
      "type": "string",
      "description": "Human note about this file. Placeholder maps should say PLACEHOLDER."
    },
    "titles": {
      "type": "object",
      "description": "Optional UI label for a bookSectionId. Example: sec-1-3-1 → Headings, div, and span.",
      "additionalProperties": { "type": "string", "minLength": 1 }
    },
    "sections": {
      "type": "object",
      "description": "bookSectionId → clips. Keys are book TOC anchors such as sec-1-3-1 and sec-2-1-10.",
      "additionalProperties": {
        "type": "array",
        "minItems": 1,
        "items": { "$ref": "#/$defs/clip" }
      }
    }
  },
  "$defs": {
    "clip": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "youtubeVideoId": {
          "type": "string",
          "pattern": "^[A-Za-z0-9_-]{11}$",
          "description": "YouTube video id. Prefer this over url."
        },
        "url": {
          "type": "string",
          "format": "uri",
          "description": "YouTube watch, embed, shorts, or youtu.be URL. Must be YouTube. Ignored as a host if it is not YouTube."
        },
        "startSec": {
          "type": "number",
          "minimum": 0,
          "description": "Clip start in seconds from the beginning of the YouTube video (from the VTT cue)."
        },
        "endSec": {
          "type": "number",
          "exclusiveMinimum": 0,
          "description": "Clip end in seconds. Shown in the UI and passed as the YouTube embed end parameter."
        },
        "sourceCourse": {
          "type": "string",
          "minLength": 1,
          "description": "Course section this recording belongs to. Fall 2026 ids: CS4550, CS5610-02, CS5610-09."
        },
        "semester": {
          "type": "string",
          "pattern": "^(SP|SU|FA)\\d{2}$",
          "description": "Season plus 2-digit year. SP26 < SU26 < FA26. Later semesters are not used as fallbacks for an earlier preferred semester."
        },
        "confidence": {
          "description": "Tie-break when several clips sit in the same fallback tier. A 0–1 score, or high, medium, low, or placeholder.",
          "oneOf": [
            {
              "type": "string",
              "enum": ["high", "medium", "low", "placeholder"]
            },
            { "type": "number", "minimum": 0, "maximum": 1 }
          ]
        },
        "note": {
          "type": "string",
          "description": "Curator note kept in git. Not rendered on the student page."
        }
      },
      "required": ["startSec", "endSec", "sourceCourse", "semester", "confidence"],
      "anyOf": [
        { "required": ["youtubeVideoId"] },
        { "required": ["url"] }
      ]
    }
  }
}
