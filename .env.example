# Sibling Express server (PDF: NEXT_PUBLIC_HTTP_SERVER).
# Local default in code if omitted: http://localhost:4000
# Production: https://your-kambaz-node-server-app.onrender.com  (no trailing slash)
NEXT_PUBLIC_HTTP_SERVER=http://localhost:4000
# Alias accepted by httpServer(): NEXT_PUBLIC_API_BASE
