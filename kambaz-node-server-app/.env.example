PORT=4000
SERVER_ENV=development
CLIENT_URL=http://localhost:3000
SERVER_URL=localhost:4000
SESSION_SECRET=super secret session phrase
# PDF name. Also accepted: MONGO_CONNECTION_STRING.
# Unset → in-memory DAOs (CI / no local mongod).
# DATABASE_CONNECTION_STRING=mongodb://127.0.0.1:27017/kambaz
